# TODO: Run the To-Do App

- [ ] Start the backend server (Express on port 4000) using 'cd backend && node index.js'
- [ ] Start the frontend dev server (React on port 3000) using 'cd frontend && npm start'
- [ ] Launch browser to http://localhost:3000 to view the app
